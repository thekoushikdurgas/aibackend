# mypy report notes (2026-05-30)
#
# Previous errors (16 in 12 files) addressed by:
# - mypy.ini: top-level [mypy-boto3], [mypy-botocore], [mypy-socketio], [mypy-docx],
#   [mypy-bs4], [mypy-pandas] (wildcard-only sections missed bare imports)
# - requirements-dev.txt already lists types-beautifulsoup4, pandas-stubs
# - app/services/video_service.py uses PIL Image.save instead of cv2.imwrite
#
# Reproduce: pip install -r requirements.txt -r requirements-dev.txt && mypy app/
# (Use Python 3.11; host Python 3.14 cannot install torch==2.2.0 from requirements.txt)
